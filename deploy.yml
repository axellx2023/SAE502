---
# Playbook principal de déploiement complet
# Installe et configure un serveur VPN WireGuard avec supervision
- name: Déploiement complet du serveur VPN WireGuard
  hosts: vpn_servers
  become: yes
  gather_facts: yes

  vars:
    # Variables de déploiement
    deployment_stage: "full"

  pre_tasks:
    - name: Vérifier que le système est Ubuntu
      assert:
        that:
          - ansible_distribution == "Ubuntu"
        fail_msg: "Ce playbook est conçu pour Ubuntu uniquement"
        success_msg: "Système Ubuntu détecté"

    - name: Mettre à jour le cache des paquets
      apt:
        update_cache: yes
        cache_valid_time: 3600

  roles:
    - role: system
      tags: system
    - role: firewall
      tags: firewall
    - role: wireguard
      tags: wireguard
    - role: docker
      tags: docker
    - role: nginx
      tags: nginx

  post_tasks:
    - name: Afficher un résumé du déploiement
      debug:
        msg:
          - ""
          - "═══════════════════════════════════════════════════════════════"
          - "✅ DÉPLOIEMENT TERMINÉ AVEC SUCCÈS !"
          - "═══════════════════════════════════════════════════════════════"
          - ""
          - "🔐 WireGuard VPN"
          - "   • Interface : {{ wireguard_interface }}"
          - "   • Port : {{ wireguard_port }}/udp"
          - "   • Réseau VPN : {{ wireguard_network }}"
          - "   • IP serveur VPN : {{ wireguard_server_ip }}"
          - "   • Endpoint (4G/externe) : {{ wireguard_effective_endpoint_ip }}:{{ wireguard_port }} (détection auto)"
          - ""
          - "🌐 Services accessibles depuis l'extérieur (LAN) :"
          - "   • WebUI      : http://{{ ansible_default_ipv4.address }}:80"
          - "     (Gestion des configurations VPN - authentification Basic Auth: admin/admin)"
          - ""
          - "🔒 Services internes (accessibles via VPN selon le rôle) :"
          - "   • Service web interne : http://{{ wireguard_server_ip }}:8080"
          - "   • Grafana : http://{{ wireguard_server_ip }}:{{ grafana_port }}"
          - "   • Prometheus : http://{{ wireguard_server_ip }}:{{ prometheus_port }} (admin uniquement)"
          - ""
          - "👥 Rôles : restricted (VPN seul) | allowed (VPN + services) | admin (accès complet)"
          - ""
          - "📊 Supervision : Prometheus http://localhost:{{ prometheus_port }} | Grafana http://localhost:{{ grafana_port }}"
          - ""
          - "📱 Prochaines étapes :"
          - "   • Créer un client : ansible-playbook add_user.yml"
          - "   • Les configs client ont l'Endpoint à jour (4G) à chaque deploy"
          - ""
          - "═══════════════════════════════════════════════════════════════"
