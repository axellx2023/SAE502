---
# Playbook interactif pour ajouter un utilisateur VPN WireGuard
# Usage: ansible-playbook add_user.yml
# Le playbook demande interactivement le nom d'utilisateur et le rôle
- name: Ajout interactif d'un utilisateur VPN WireGuard
  hosts: vpn_servers
  become: yes
  gather_facts: yes

  vars_prompt:
    - name: client_name
      prompt: "Nom du client VPN (espaces → tirets automatiquement) [default: user1]"
      private: no
      default: "user1"

    - name: user_role
      prompt: "Rôle du client - restricted (VPN seul), allowed (VPN + services internes), admin (accès complet) [default: allowed]"
      private: no
      default: "allowed"

  pre_tasks:
    - name: Définir l'IP effective pour l'Endpoint WireGuard (variable globale)
      ansible.builtin.set_fact:
        wireguard_effective_endpoint_ip: "{{ wireguard_server_public_ip | default(ansible_default_ipv4.address, true) }}"
      tags:
        - adduser
        - config

    - name: Normaliser le nom du client (remplacer espaces par tirets)
      ansible.builtin.set_fact:
        client_name: "{{ client_name | replace(' ', '-') | lower }}"
      tags:
        - adduser
        - config

    - name: Vérifier que le nom du client est valide
      assert:
        that:
          - client_name | length > 0
          - client_name | regex_search('^[a-zA-Z0-9_-]+$')
        fail_msg: "Le nom du client doit contenir uniquement des lettres, chiffres, tirets et underscores"
        success_msg: "Nom du client valide (normalisé : {{ client_name }})"

    - name: Vérifier que le rôle est valide
      assert:
        that:
          - user_role in ['restricted', 'allowed', 'admin']
        fail_msg: "Le rôle doit être 'restricted', 'allowed' ou 'admin'"
        success_msg: "Rôle valide : {{ user_role }}"

    - name: Vérifier que le client n'existe pas déjà
      stat:
        path: "{{ wireguard_clients_path }}/{{ client_name }}.conf"
      register: client_exists

    - name: Arrêter si le client existe déjà
      ansible.builtin.fail:
        msg: |
          ❌ Le client '{{ client_name }}' existe déjà.
          
          Pour recréer ce client :
          1. Supprimez le fichier : {{ wireguard_clients_path }}/{{ client_name }}.conf
          2. Supprimez le QR code : {{ wireguard_qr_path }}/{{ client_name }}.png
          3. Supprimez le bloc [Peer] correspondant dans {{ wireguard_config_path }}/{{ wireguard_interface }}.conf
          4. Rechargez WireGuard : wg syncconf {{ wireguard_interface }} {{ wireguard_config_path }}/{{ wireguard_interface }}.conf
          5. Relancez ce playbook
          
          OU choisissez un autre nom de client.
      when: client_exists.stat.exists

    - name: Extraire les IPs utilisées depuis wg0.conf
      ansible.builtin.shell: |
        grep -E "AllowedIPs = 10\.10\.10\.[0-9]+/32" {{ wireguard_config_path }}/{{ wireguard_interface }}.conf 2>/dev/null | \
        sed 's/.*AllowedIPs = 10\.10\.10\.\([0-9]*\)\/32.*/\1/' | \
        sort -nu
      register: used_ips_wg0
      changed_when: false
      failed_when: false

    - name: Extraire les IPs utilisées depuis les fichiers clients .conf
      ansible.builtin.shell: |
        grep -hE "^Address = 10\.10\.10\.[0-9]+/32" {{ wireguard_clients_path }}/*.conf 2>/dev/null | \
        sed 's/.*Address = 10\.10\.10\.\([0-9]*\)\/32.*/\1/' | \
        sort -nu
      register: used_ips_clients
      changed_when: false
      failed_when: false

    - name: Fusionner toutes les IPs utilisées
      ansible.builtin.set_fact:
        all_used_ips: "{{ (used_ips_wg0.stdout_lines | default([])) + (used_ips_clients.stdout_lines | default([])) | unique | map('int') | list | sort }}"
      tags:
        - adduser
        - config

    - name: Calculer le prochain IP disponible
      ansible.builtin.set_fact:
        next_ip_num: "{{ range(2, 255) | reject('in', all_used_ips | default([])) | first | default(254) }}"
      tags:
        - adduser
        - config

    - name: Vérifier que l'IP calculée est dans la plage valide
      ansible.builtin.assert:
        that:
          - next_ip_num | int >= 2
          - next_ip_num | int <= 254
          - next_ip_num | int not in (all_used_ips | default([]))
        fail_msg: "Impossible de trouver une IP disponible dans le réseau 10.10.10.0/24 (253 clients maximum). IPs déjà utilisées : {{ all_used_ips | default([]) | join(', ') }}"
        success_msg: "IP disponible trouvée : 10.10.10.{{ next_ip_num }}"

    - name: Définir l'IP du client
      ansible.builtin.set_fact:
        user_ip: "10.10.10.{{ next_ip_num }}"

  roles:
    - role: adduser
      tags: adduser

  post_tasks:
    - name: Afficher un résumé de la création
      debug:
        msg:
          - ""
          - "═══════════════════════════════════════════════════════════════"
          - "✅ Client VPN créé avec succès !"
          - "═══════════════════════════════════════════════════════════════"
          - ""
          - "👤 Nom du client : {{ client_name }}"
          - "🌐 IP VPN attribuée  : {{ user_ip }}"
          - "🔐 Rôle : {{ user_role }}"
          - ""
          - "📁 Fichiers créés :"
          - "   • Configuration : {{ wireguard_clients_path }}/{{ client_name }}.conf"
          - "   • QR Code       : {{ wireguard_qr_path }}/{{ client_name }}.png"
          - ""
          - "📱 Pour utiliser le VPN :"
          - "   1. Téléchargez le fichier .conf ou scannez le QR code"
          - "   2. Importez-le dans votre client WireGuard"
          - "   3. Activez la connexion VPN"
          - ""
          - "🔗 Accès aux fichiers :"
          - "   • WebUI : http://{{ ansible_default_ipv4.address }}:{{ webui_port }}"
          - ""
          - "🌐 Endpoint WireGuard : {{ wireguard_effective_endpoint_ip | default('Détection...') }}:{{ wireguard_port }}"
          - "   ({{ 'IP publique configurée' if wireguard_server_public_ip | default('') != '' else 'IP locale détectée automatiquement' }})"
          - ""
          - "🌍 Accès selon le rôle :"
          - "   • restricted : VPN uniquement (aucun service)"
          - "   • allowed : VPN + services internes (Nginx, Grafana)"
          - "   • admin : Accès complet (tous les services)"
          - ""
          - "📊 Services disponibles :"
          - "   • Nginx interne : http://{{ wireguard_server_ip }}:{{ nginx_port }}"
          - "   • Grafana : http://{{ wireguard_server_ip }}:{{ grafana_port }}"
          - "   • Prometheus : http://{{ wireguard_server_ip }}:{{ prometheus_port }} (admin uniquement)"
          - ""
          - "═══════════════════════════════════════════════════════════════"
