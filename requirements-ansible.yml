---
# Collections Ansible nécessaires
# Installer avec: ansible-galaxy collection install -r requirements-ansible.yml

collections:
  - name: community.docker
    version: ">=3.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
