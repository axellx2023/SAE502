<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Service Interne - VPN WireGuard</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }
        .container {
            background: white;
            border-radius: 15px;
            padding: 40px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.3);
            max-width: 800px;
            width: 100%;
        }
        h1 {
            color: #667eea;
            margin-bottom: 20px;
            font-size: 2.5em;
            border-bottom: 3px solid #764ba2;
            padding-bottom: 15px;
        }
        .success-badge {
            display: inline-block;
            background: #28a745;
            color: white;
            padding: 8px 16px;
            border-radius: 20px;
            font-weight: bold;
            margin-bottom: 20px;
        }
        .info-section {
            background: #f8f9fa;
            border-left: 4px solid #667eea;
            padding: 20px;
            margin: 20px 0;
            border-radius: 5px;
        }
        .info-section h2 {
            color: #667eea;
            margin-bottom: 10px;
            font-size: 1.5em;
        }
        .info-section p {
            line-height: 1.6;
            color: #333;
            margin-bottom: 10px;
        }
        .warning-box {
            background: #fff3cd;
            border: 1px solid #ffc107;
            border-left: 4px solid #ffc107;
            padding: 15px;
            margin: 20px 0;
            border-radius: 5px;
        }
        .warning-box strong {
            color: #856404;
        }
        .footer {
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #e0e0e0;
            color: #666;
            font-size: 0.9em;
            text-align: center;
        }
        ul {
            margin-left: 20px;
            margin-top: 10px;
        }
        li {
            margin-bottom: 8px;
            line-height: 1.5;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>🔒 Service Interne Protégé</h1>
        <span class="success-badge">✅ Accès VPN autorisé</span>
        
        <div class="info-section">
            <h2>Bienvenue sur le service interne</h2>
            <p>Vous avez accès à ce service car vous êtes connecté via le VPN WireGuard avec un rôle <strong>"allowed"</strong>.</p>
            <p>Ce service n'est accessible que depuis le réseau VPN ({{ wireguard_network }}).</p>
        </div>

        <div class="warning-box">
            <strong>⚠️ Sécurité :</strong>
            <ul>
                <li>Ce service est protégé par le firewall UFW et n'est accessible que depuis le réseau VPN ({{ wireguard_network }})</li>
                <li>Seuls les clients VPN avec le rôle <strong>"allowed"</strong> peuvent accéder aux services web internes (ports 80 et 443)</li>
                <li>Les clients avec le rôle <strong>"denied"</strong> ne peuvent pas accéder à ce service, même connectés au VPN</li>
            </ul>
        </div>

        <div class="info-section">
            <h2>Informations de connexion</h2>
            <p><strong>Votre IP VPN :</strong> <code>{{ ansible_default_ipv4.address | default('Détection en cours...') }}</code></p>
            <p><strong>Serveur VPN :</strong> {{ wireguard_server_ip }}</p>
            <p><strong>Réseau VPN :</strong> {{ wireguard_network }}</p>
        </div>

        <div class="info-section">
            <h2>Services disponibles</h2>
            <ul>
                <li><strong>Service web interne :</strong> http://{{ wireguard_server_ip }}:{{ nginx_port }} (ce service)</li>
                <li><strong>WebUI (extérieur) :</strong> http://{{ ansible_default_ipv4.address | default('IP') }}:{{ webui_port }} (gestion des configurations VPN)</li>
                <li><strong>Prometheus (localhost) :</strong> http://localhost:{{ prometheus_port }} (supervision)</li>
                <li><strong>Grafana (localhost + VPN) :</strong> http://localhost:{{ grafana_port }} (visualisation)</li>
            </ul>
        </div>

        <div class="footer">
            <p>Service déployé automatiquement via Ansible - SAE502</p>
            <p>Dernière mise à jour : {{ ansible_date_time.iso8601 | default('N/A') }}</p>
        </div>
    </div>
</body>
</html>
