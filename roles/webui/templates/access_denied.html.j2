<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Accès refusé</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        :root {
            --navy: #0a1929;
            --navy-dark: #05141f;
            --navy-light: #132f4c;
            --steel: #1e3a5f;
            --steel-light: #2a4a6b;
            --accent: #00d4ff;
            --danger: #ff4444;
            --text-primary: #e0e7ff;
            --text-secondary: #a0aec0;
            --text-muted: #718096;
            --border: #1a365d;
            --card-bg: #0f1b2e;
            --shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
        }
        
        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: var(--navy-dark);
            min-height: 100vh;
            padding: 1.5rem;
            color: var(--text-primary);
        }
        
        .container {
            max-width: 800px;
            margin: 0 auto;
        }
        
        .card {
            background: var(--card-bg);
            border: 1px solid var(--border);
            border-radius: 8px;
            padding: 2rem;
            box-shadow: var(--shadow);
            text-align: center;
        }
        
        .title {
            font-size: 1.5rem;
            font-weight: 700;
            margin-bottom: 0.75rem;
            color: var(--danger);
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .subtitle {
            color: var(--text-secondary);
            margin-bottom: 1rem;
        }
        
        .meta {
            color: var(--text-muted);
            font-size: 0.85rem;
            margin-bottom: 1.5rem;
        }
        
        .btn {
            padding: 0.5rem 1rem;
            border: 1px solid var(--border);
            border-radius: 4px;
            cursor: pointer;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.8125rem;
            font-weight: 600;
            transition: all 0.2s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            background: var(--steel);
            color: var(--text-primary);
        }
        
        .btn:hover {
            border-color: var(--accent);
            color: var(--accent);
            box-shadow: 0 0 10px rgba(0, 212, 255, 0.3);
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="card">
            <div class="title">Accès refusé</div>
            <div class="subtitle">Cette page est réservée aux utilisateurs VPN autorisés.</div>
            <div class="meta">
                {% if username %}
                Connecté en tant que <strong>{{ username }}</strong>
                {% if user_role %}(rôle: {{ user_role }}){% endif %}
                {% else %}
                Utilisateur non identifié
                {% endif %}
            </div>
            <a href="/" class="btn">Retour à l'accueil</a>
        </div>
    </div>
</body>
</html>
