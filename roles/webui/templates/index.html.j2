<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WireGuard VPN - Command Center</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        :root {
            --navy: #0a1929;
            --navy-dark: #05141f;
            --navy-light: #132f4c;
            --steel: #1e3a5f;
            --steel-light: #2a4a6b;
            --accent: #00d4ff;
            --accent-dark: #00a8cc;
            --success: #00ff88;
            --warning: #ffaa00;
            --danger: #ff4444;
            --text-primary: #e0e7ff;
            --text-secondary: #a0aec0;
            --text-muted: #718096;
            --border: #1a365d;
            --card-bg: #0f1b2e;
            --shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
        }
        
        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: var(--navy-dark);
            background-image: 
                linear-gradient(135deg, rgba(0, 212, 255, 0.05) 0%, rgba(30, 58, 95, 0.1) 100%),
                radial-gradient(circle at 20% 50%, rgba(0, 212, 255, 0.03) 0%, transparent 50%);
            min-height: 100vh;
            padding: 1.5rem;
            color: var(--text-primary);
            line-height: 1.6;
        }
        
        .container {
            max-width: 1600px;
            margin: 0 auto;
        }
        
        .header {
            background: var(--navy);
            border: 1px solid var(--border);
            border-radius: 8px;
            padding: 1.5rem 2rem;
            margin-bottom: 1.5rem;
            box-shadow: var(--shadow);
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 1rem;
        }
        
        .header-left {
            display: flex;
            align-items: center;
            gap: 1rem;
        }
        
        .header-icon {
            width: 48px;
            height: 48px;
            background: linear-gradient(135deg, var(--accent), var(--accent-dark));
            border-radius: 6px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            box-shadow: 0 0 20px rgba(0, 212, 255, 0.3);
        }
        
        .header-title {
            display: flex;
            flex-direction: column;
        }
        
        .header-title h1 {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--text-primary);
            letter-spacing: 0.5px;
            text-transform: uppercase;
        }
        
        .header-title .subtitle {
            font-size: 0.75rem;
            color: var(--text-muted);
            letter-spacing: 1px;
            text-transform: uppercase;
            margin-top: 0.25rem;
        }

        .header-actions {
            display: flex;
            gap: 0.5rem;
            flex-wrap: wrap;
        }
        
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1rem;
            margin-bottom: 1.5rem;
        }
        
        .stat-card {
            background: var(--card-bg);
            border: 1px solid var(--border);
            border-radius: 6px;
            padding: 1.25rem;
            position: relative;
            overflow: hidden;
        }
        
        .stat-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 3px;
            background: linear-gradient(90deg, var(--accent), var(--accent-dark));
        }
        
        .stat-label {
            font-size: 0.75rem;
            color: var(--text-muted);
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 0.5rem;
            font-weight: 600;
        }
        
        .stat-value {
            font-size: 2rem;
            font-weight: 700;
            color: var(--accent);
            font-variant-numeric: tabular-nums;
        }
        
        .client-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(340px, 1fr));
            gap: 1rem;
        }
        
        .client-card {
            background: var(--card-bg);
            border: 1px solid var(--border);
            border-radius: 6px;
            padding: 1.5rem;
            transition: all 0.3s ease;
            position: relative;
        }
        
        .client-card:hover {
            border-color: var(--accent);
            box-shadow: 0 0 20px rgba(0, 212, 255, 0.2);
            transform: translateY(-2px);
        }
        
        .client-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 1rem;
            padding-bottom: 1rem;
            border-bottom: 1px solid var(--border);
        }
        
        .client-name {
            font-size: 1.125rem;
            font-weight: 600;
            color: var(--text-primary);
            display: flex;
            align-items: center;
            gap: 0.5rem;
            letter-spacing: 0.3px;
        }
        
        .client-name::before {
            content: '▸';
            color: var(--accent);
            font-size: 1.25rem;
        }
        
        .status-badges {
            display: flex;
            gap: 0.5rem;
            flex-wrap: wrap;
            margin-bottom: 1rem;
        }
        
        .badge {
            display: inline-flex;
            align-items: center;
            gap: 0.375rem;
            padding: 0.375rem 0.75rem;
            border-radius: 4px;
            font-size: 0.6875rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            border: 1px solid;
        }
        
        .badge-success {
            background: rgba(0, 255, 136, 0.1);
            color: var(--success);
            border-color: rgba(0, 255, 136, 0.3);
        }
        
        .badge-danger {
            background: rgba(255, 68, 68, 0.1);
            color: var(--danger);
            border-color: rgba(255, 68, 68, 0.3);
        }
        
        .client-actions {
            display: flex;
            gap: 0.5rem;
            flex-wrap: wrap;
            margin-top: 1rem;
        }
        
        .btn {
            padding: 0.5rem 1rem;
            border: 1px solid;
            border-radius: 4px;
            cursor: pointer;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.8125rem;
            font-weight: 600;
            transition: all 0.2s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .btn-primary {
            background: var(--steel);
            color: var(--text-primary);
            border-color: var(--steel-light);
        }
        
        .btn-primary:hover {
            background: var(--steel-light);
            border-color: var(--accent);
            color: var(--accent);
            box-shadow: 0 0 10px rgba(0, 212, 255, 0.3);
        }
        
        .btn-success {
            background: rgba(0, 255, 136, 0.1);
            color: var(--success);
            border-color: rgba(0, 255, 136, 0.3);
        }
        
        .btn-success:hover {
            background: rgba(0, 255, 136, 0.2);
            box-shadow: 0 0 10px rgba(0, 255, 136, 0.3);
        }
        
        .qr-preview {
            margin-top: 1rem;
            padding-top: 1rem;
            border-top: 1px solid var(--border);
            text-align: center;
        }
        
        .qr-preview img {
            max-width: 160px;
            border: 2px solid var(--border);
            border-radius: 4px;
            padding: 0.5rem;
            background: var(--navy);
        }
        
        .empty-state {
            background: var(--card-bg);
            border: 1px solid var(--border);
            border-radius: 6px;
            padding: 4rem 2rem;
            text-align: center;
        }
        
        .empty-state-icon {
            font-size: 4rem;
            margin-bottom: 1rem;
            opacity: 0.3;
        }
        
        .empty-state h2 {
            font-size: 1.25rem;
            color: var(--text-primary);
            margin-bottom: 0.5rem;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .empty-state p {
            color: var(--text-muted);
            margin-bottom: 1rem;
        }
        
        .code-block {
            background: var(--navy);
            border: 1px solid var(--border);
            padding: 0.75rem 1rem;
            border-radius: 4px;
            font-family: 'Courier New', monospace;
            font-size: 0.8125rem;
            color: var(--accent);
            display: inline-block;
            margin-top: 0.5rem;
        }
        
        @media (max-width: 768px) {
            .header {
                padding: 1rem;
            }
            
            .header-title h1 {
                font-size: 1.25rem;
            }
            
            .client-grid {
                grid-template-columns: 1fr;
            }
            
            .stats-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="header-left">
                <div class="header-icon">⚡</div>
                <div class="header-title">
                    <h1>WireGuard VPN</h1>
                    <div class="subtitle">Command & Control Center</div>
                </div>
            </div>
            <div class="header-actions">
                <a href="/interne" class="btn btn-primary">Page interne</a>
            </div>
        </div>
        
        {% if clients %}
        <div class="stats-grid">
            <div class="stat-card">
                <div class="stat-label">Active Clients</div>
                <div class="stat-value">{{ clients|length }}</div>
            </div>
            <div class="stat-card">
                <div class="stat-label">Configurations</div>
                <div class="stat-value">{{ clients|selectattr('conf_exists')|list|length }}</div>
            </div>
            <div class="stat-card">
                <div class="stat-label">QR Codes</div>
                <div class="stat-value">{{ clients|selectattr('qr_exists')|list|length }}</div>
            </div>
        </div>
        
        <div class="client-grid">
            {% for client in clients %}
            <div class="client-card">
                <div class="client-header">
                    <div class="client-name">{{ client.name }}</div>
                </div>
                
                <div class="status-badges">
                    {% if client.conf_exists %}
                    <span class="badge badge-success">✓ Config</span>
                    {% else %}
                    <span class="badge badge-danger">✗ Config</span>
                    {% endif %}
                    
                    {% if client.qr_exists %}
                    <span class="badge badge-success">✓ QR</span>
                    {% else %}
                    <span class="badge badge-danger">✗ QR</span>
                    {% endif %}
                </div>
                
                <div class="client-actions">
                    {% if client.conf_exists %}
                    <a href="/client/{{ client.name }}" class="btn btn-primary">View</a>
                    <a href="/download/{{ client.name }}" class="btn btn-primary">Download</a>
                    {% endif %}
                    
                    {% if client.qr_exists %}
                    <a href="/qr/{{ client.name }}" target="_blank" class="btn btn-success">QR Code</a>
                    {% endif %}
                </div>
                
                {% if client.qr_exists %}
                <div class="qr-preview">
                    <img src="/qr/{{ client.name }}" alt="QR Code {{ client.name }}" loading="lazy">
                </div>
                {% endif %}
            </div>
            {% endfor %}
        </div>
        {% else %}
        <div class="empty-state">
            <div class="empty-state-icon">⚙</div>
            <h2>No Active Clients</h2>
            <p>Initialize VPN client configuration</p>
            <div class="code-block">ansible-playbook -i inventory.ini add_user.yml --ask-become-pass</div>
        </div>
        {% endif %}
    </div>
</body>
</html>
