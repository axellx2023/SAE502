<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Page interne - WireGuard VPN</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        :root {
            --navy: #0a1929;
            --navy-dark: #05141f;
            --navy-light: #132f4c;
            --steel: #1e3a5f;
            --steel-light: #2a4a6b;
            --accent: #00d4ff;
            --accent-dark: #00a8cc;
            --success: #00ff88;
            --warning: #ffaa00;
            --danger: #ff4444;
            --text-primary: #e0e7ff;
            --text-secondary: #a0aec0;
            --text-muted: #718096;
            --border: #1a365d;
            --card-bg: #0f1b2e;
            --shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
        }
        
        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: var(--navy-dark);
            background-image: 
                linear-gradient(135deg, rgba(0, 212, 255, 0.05) 0%, rgba(30, 58, 95, 0.1) 100%),
                radial-gradient(circle at 20% 50%, rgba(0, 212, 255, 0.03) 0%, transparent 50%);
            min-height: 100vh;
            padding: 1.5rem;
            color: var(--text-primary);
            line-height: 1.6;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
        }
        
        .header {
            background: var(--navy);
            border: 1px solid var(--border);
            border-radius: 8px;
            padding: 1.5rem 2rem;
            margin-bottom: 1.5rem;
            box-shadow: var(--shadow);
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 1rem;
        }
        
        .header-left {
            display: flex;
            align-items: center;
            gap: 1rem;
        }
        
        .header-icon {
            width: 48px;
            height: 48px;
            background: linear-gradient(135deg, var(--accent), var(--accent-dark));
            border-radius: 6px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            box-shadow: 0 0 20px rgba(0, 212, 255, 0.3);
        }
        
        .header-title h1 {
            font-size: 1.4rem;
            font-weight: 700;
            color: var(--text-primary);
            letter-spacing: 0.5px;
            text-transform: uppercase;
        }
        
        .header-subtitle {
            font-size: 0.75rem;
            color: var(--text-muted);
            letter-spacing: 1px;
            text-transform: uppercase;
            margin-top: 0.25rem;
        }
        
        .header-actions {
            display: flex;
            gap: 0.5rem;
            flex-wrap: wrap;
        }
        
        .btn {
            padding: 0.5rem 1rem;
            border: 1px solid;
            border-radius: 4px;
            cursor: pointer;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.8125rem;
            font-weight: 600;
            transition: all 0.2s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .btn-secondary {
            background: var(--card-bg);
            color: var(--text-secondary);
            border-color: var(--border);
        }
        
        .btn-secondary:hover {
            border-color: var(--accent);
            color: var(--accent);
        }
        
        .section {
            background: var(--card-bg);
            border: 1px solid var(--border);
            border-radius: 6px;
            padding: 1.5rem;
            margin-bottom: 1.5rem;
            box-shadow: var(--shadow);
        }
        
        .section-title {
            font-size: 0.875rem;
            font-weight: 600;
            color: var(--accent);
            margin-bottom: 1rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .section-title::before {
            content: '▸';
            color: var(--accent);
        }
        
        .meta {
            color: var(--text-muted);
            font-size: 0.8125rem;
        }
        
        .list {
            margin-top: 0.75rem;
            padding-left: 1.25rem;
            color: var(--text-secondary);
        }
        
        .list li {
            margin-bottom: 0.5rem;
        }
        
        .badge {
            display: inline-flex;
            align-items: center;
            gap: 0.375rem;
            padding: 0.25rem 0.5rem;
            border-radius: 4px;
            font-size: 0.6875rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            border: 1px solid rgba(0, 255, 136, 0.3);
            color: var(--success);
            background: rgba(0, 255, 136, 0.1);
        }
        
        @media (max-width: 768px) {
            .header {
                padding: 1rem;
            }
            
            .header-title h1 {
                font-size: 1.1rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="header-left">
                <div class="header-icon">📌</div>
                <div>
                    <h1>Page interne</h1>
                    <div class="header-subtitle">Objectif & compte rendu</div>
                </div>
            </div>
            <div class="header-actions">
                <a href="/" class="btn btn-secondary">← Retour</a>
            </div>
        </div>

        <div class="section">
            <h2 class="section-title">Accès</h2>
            <p class="meta">Connecté en tant que <strong>{{ username }}</strong> (rôle: {{ user_role }})</p>
            <p class="meta" style="margin-top: 0.5rem;">
                Cette page est réservée aux utilisateurs VPN avec le rôle <span class="badge">allowed/admin</span>.
            </p>
        </div>
        
        <div class="section">
            <h2 class="section-title">Objectif du projet</h2>
            <p>
                Mettre en place une infrastructure VPN WireGuard sécurisée, automatisée par Ansible,
                avec une gestion des utilisateurs et des accès différenciés selon les rôles.
            </p>
            <ul class="list">
                <li>Accès VPN stable pour les clients internes et externes.</li>
                <li>Accès aux services web internes selon les droits (allowed/admin).</li>
                <li>Supervision et observabilité via des services dédiés.</li>
            </ul>
        </div>

        <div class="section">
            <h2 class="section-title">Compte rendu</h2>
            <p>
                Le projet met en place un environnement complet de gestion VPN, avec une interface Web
                dédiée et des règles de sécurité adaptées aux rôles utilisateurs.
            </p>
            <ul class="list">
                <li>Déploiement automatisé des services via Ansible et Docker.</li>
                <li>Protection des accès web par authentification Basic Auth.</li>
                <li>Gestion des rôles utilisateurs (restricted, allowed, admin) et filtrage UFW.</li>
                <li>Génération des configurations clients et QR codes pour faciliter l'onboarding.</li>
            </ul>
        </div>
    </div>
</body>
</html>
