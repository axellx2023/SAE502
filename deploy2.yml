---
# deploy2.yml – Tout-en-un : test de connexion + déploiement complet
#
# 1. Configurer une seule fois inventory.ini :
#    [vpn_servers]
#    192.168.101.137
#
#    [vpn_servers:vars]
#    ansible_user=ubuntu
#    ansible_ssh_common_args='-o StrictHostKeyChecking=no'
#    ansible_python_interpreter=/usr/bin/python3
#
# 2. Lancer : ansible-playbook deploy2.yml
#    (ou ansible-playbook deploy2.yml --ask-become-pass si sudo demande un mot de passe)

- name: "Étape 1 – Tester la connexion (ping)"
  hosts: vpn_servers
  gather_facts: false
  tasks:
    - name: Vérifier que les hôtes sont joignables
      ansible.builtin.ping:
      register: ping_result
      failed_when: false

    - name: Échouer si le serveur ne répond pas
      ansible.builtin.fail:
        msg: |
          Impossible de joindre le serveur {{ inventory_hostname }}.
          Vérifiez inventory.ini (IP ou hostname) et la connexion SSH.
      when: ping_result.ping | default('') != 'pong'

    - name: Connexion OK
      ansible.builtin.debug:
        msg: "Connexion OK vers {{ inventory_hostname }} (pong)"

- name: "Étape 2 – Déploiement complet (système, firewall, WireGuard, Docker, Nginx)"
  hosts: vpn_servers
  become: true
  gather_facts: true
  vars:
    deployment_stage: "full"

  pre_tasks:
    - name: Vérifier que le système est Ubuntu
      ansible.builtin.assert:
        that:
          - ansible_distribution == "Ubuntu"
        fail_msg: "Ce playbook est conçu pour Ubuntu uniquement"
        success_msg: "Système Ubuntu détecté"

    - name: Mettre à jour le cache des paquets
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 3600

  roles:
    - role: system
      tags: system
    - role: firewall
      tags: firewall
    - role: wireguard
      tags: wireguard
    - role: docker
      tags: docker
    - role: nginx
      tags: nginx

  post_tasks:
    - name: Afficher un résumé du déploiement
      ansible.builtin.debug:
        msg:
          - ""
          - "═══════════════════════════════════════════════════════════════"
          - "✅ DÉPLOIEMENT TERMINÉ AVEC SUCCÈS !"
          - "═══════════════════════════════════════════════════════════════"
          - ""
          - "🔐 WireGuard : {{ wireguard_effective_endpoint_ip }}:{{ wireguard_port }} (Endpoint 4G auto)"
          - "🌐 WebUI : http://{{ ansible_default_ipv4.address }}:80"
          - "📱 Prochaines étapes : ansible-playbook add_user.yml"
          - ""
          - "═══════════════════════════════════════════════════════════════"
